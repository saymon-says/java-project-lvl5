server:
  error:
    include-stacktrace: never

spring:
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false

  datasource:
    url: ${DATABASE_URL}

  jpa:
    show-sql: false

logging:
  level:
    ROOT: INFO